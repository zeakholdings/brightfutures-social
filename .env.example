DIRECTUS_URL=https://cms.brightfutures.social
# Setup and seed scripts only. Never expose this value to browser code.
DIRECTUS_ADMIN_TOKEN=
# Runtime server only. Use a dedicated policy that can create contact_messages and ideas only.
DIRECTUS_SERVER_TOKEN=
